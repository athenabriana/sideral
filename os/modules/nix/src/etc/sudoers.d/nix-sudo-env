# Add nix profile bin directory to sudo secure_path so that commands
# installed via `nix profile install` are found when running with sudo.
Defaults    secure_path = /sbin:/bin:/usr/sbin:/usr/bin:/nix/var/nix/profiles/default/bin
