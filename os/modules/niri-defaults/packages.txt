# desktop-niri — niri compositor + Noctalia shell + supporting tools.
#
# Source split:
#   • Fedora main: niri, sddm, sddm-wayland-generic, kanshi, wdisplays,
#     ddcutil, brightnessctl, fastfetch, wlsunset, fprintd, fcitx5,
#     fcitx5-configtool, grim, slurp, wl-clipboard
#   • Terra (via /etc/yum.repos.d/terra.repo, staged by build.sh's
#     persistent-repo step before this install runs):
#     noctalia-shell, noctalia-qs, ghostty, cliphist, matugen, kanata
#     (rust-matugen is the source RPM name; binary package is matugen)
#
# noctalia-qs Conflicts: quickshell + Provides: quickshell — it's the
# Quickshell hard-fork that Noctalia targets exclusively starting v4.6.0.
# Sideral does not ship upstream Quickshell.

niri
sddm
sddm-wayland-generic
kanshi
wdisplays
ddcutil
fastfetch
wlsunset
fprintd
brightnessctl
fcitx5
fcitx5-configtool
grim
slurp
wl-clipboard
cliphist
matugen
noctalia-shell
noctalia-qs
ghostty
# kanata — kernel-level key remapper (uinput). Used for Super tap-vs-hold:
# tap → Mod+Space (launcher), hold → normal Super modifier.
kanata
