# GNOME Shell extensions (RPM-layered) + theme / quality-of-life.
# Runtime extensions installed via extensions.gnome.org are handled
# by extensions.sh in this same module dir.

# ── Shell extensions (Fedora main) ──
gnome-shell-extension-appindicator
gnome-shell-extension-dash-to-panel

# ── App store ──
# Bazaar (Flathub) handles flatpak app discovery — see modules/flatpaks/
# manifest. gnome-software + gnome-software-rpm-ostree dropped 2026-05-02
# matching bluefin's direction; OS update notifications come from
# ublue-os-update-services (inherited from silverblue-main), and RPM
# layering is done via the rpm-ostree CLI when needed. The dnf-prune
# in os/lib/build.sh actively removes them from the inherited base.

# ── Quality-of-life ──
gnome-tweaks
adw-gtk3-theme
fastfetch
