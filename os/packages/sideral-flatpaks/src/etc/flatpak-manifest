# Flatpaks preinstalled at image build by os/build.sh and self-healed on
# every boot by sideral-flatpak-install.service.
# Format:
#   <remote> <ref>           — install <ref> from named remote
# Lines starting with # and blank lines are ignored.

# ── Browser ──
# Helium is installed separately by os/build.sh from MarioGK's release
# bundle (.flatpak file from GitHub Releases). The MarioGK ostree remote
# at mariogk.github.io currently serves an empty repo, so the bundle is
# the only working install path. Image rebuilds refresh helium; other
# curated flatpaks below update via standard `flatpak update`.

# ── GNOME quality-of-life ──
flathub com.github.tchx84.Flatseal
flathub io.github.flattool.Warehouse
flathub com.mattjakeman.ExtensionManager
flathub io.podman_desktop.PodmanDesktop
flathub com.ranfdev.DistroShelf
flathub net.nokyan.Resources
flathub it.mijorus.smile
