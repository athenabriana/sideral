# CLI / dev tooling not covered by silverblue-main base or mise.
# mise itself is NOT layered — it ships via home.packages in
# ~/.config/home-manager/home.nix and is materialized by
# athens-home-manager-setup.service on first login.

# gh + starship are NOT layered — shipped via home.nix
# (programs.gh / programs.starship).  Plain CLIs belong in the user layer.
#
# gcc + make + cmake are NOT layered either — they ship via home.packages
# in home.nix so the full compiler lineage (mise-built runtimes + C
# extensions compiled by pip/npm/cargo) stays consistent with nix's glibc.
# For ad-hoc builds against system libs: nix-shell -p <lib>.dev

# ── Git ergonomics ──
git-credential-libsecret
git-subtree
git-lfs

# VS Code is NOT layered — it ships via programs.vscode in home.nix so
# users can remove it with a one-line edit + `home-manager switch`.
