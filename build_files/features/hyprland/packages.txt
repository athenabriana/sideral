# Hyprland compositor, session glue, bar/shell stack, and workflow utilities
# that only make sense because we're running Hyprland.
# astal-gtk4 is built from source in post-install.sh.

# ── Compositor + session ──
hyprland
hyprpaper
hyprlock
hypridle
hyprpolkitagent
hyprcursor
hyprshot
hyprpicker
xdg-desktop-portal-hyprland

# ── Launcher, power menu, notifications, AGS framework ──
# (waybar dropped — migrated to AGS)
rofi-wayland
wlogout
SwayNotificationCenter
astal
astal-libs
astal-gjs
aylurs-gtk-shell2

# ── Hyprland workflow utilities (bound to keybinds / used by our scripts) ──
brightnessctl
playerctl
cliphist
grim
slurp
swappy
